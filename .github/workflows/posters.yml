# ══════════════════════════════════════════════
#  PORTADAS — descarga local de pósters
#  (TMDB si hay API key; AniList gratis como respaldo)
#  Se ejecuta 1 vez al día. Sin dependencias npm.
#  Para reintentar los fallidos: lanzar con
#  FORCE_POSTERS=1 en los env del step.
#  CORREGIDO: tras descargar, regenera los índices lite
#  para que la app vea las portadas locales de inmediato.
# ══════════════════════════════════════════════
name: Sincronizar portadas

on:
  schedule:
    - cron: '15 3 * * *'      # cada día a las 3:15
  workflow_dispatch:

concurrency:
  group: sync-posters
  cancel-in-progress: false

jobs:
  posters:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Buscar y descargar portadas
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: node poster-sync.js

      - name: Regenerar índices lite y fichas
        if: always()
        run: |
          node split-catalog.js public/data/catalog-donghualife.json
          node split-catalog.js public/data/catalog-donghuasub.json
          node split-catalog.js public/data/catalog-donghuaworld.json
          node split-catalog.js public/data/catalog-peliculas.json
          node split-catalog.js public/data/catalog-doramas.json

      - name: Guardar portadas
        if: always()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -A 'public/img/posters' 'public/data/catalog-donghualife*' 'public/data/catalog-donghuasub*' 'public/data/catalog-donghuaworld*' 'public/data/catalog-peliculas*' 'public/data/catalog-doramas*'
          git diff --staged --quiet || git commit -m "posters: actualizar portadas e índices"
          git pull --rebase --autostash origin main
          git push
